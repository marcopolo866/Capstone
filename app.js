// Deprecated monolithic entrypoint.
// Frontend runtime code now lives in ordered chunks under `js/app/` and is loaded directly by `index.html`.
// See `docs/refactor-split-structure.md` for load order and validation commands.
